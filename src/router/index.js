import { createRouter } from 'vue-router'

const routes = [
    // {
    //     path: '/',
    //     alias: '/home',
    //     name: 'home',
    //     component: HomeView
    // }
]

const router = createRouter({
    routes
})

export default router
